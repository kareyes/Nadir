import Root, {
	type LoadingProps,
	type LoadingAlignment,
	type LoadingSize,
	type LoadingColor,
	type LoadingVariant,
	loadingVariants,
} from "./loading.svelte";

export {
	Root,
	type LoadingProps as Props,
	//
	Root as Loading,
	loadingVariants,
	type LoadingProps,
	type LoadingAlignment,
	type LoadingSize,
	type LoadingColor,
	type LoadingVariant,
};